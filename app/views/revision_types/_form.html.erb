<%= form_with(model: revision_type, local: true) do |form| %>
  <% if revision_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(revision_type.errors.count, "error") %> prohibited this revision_type from being saved:</h2>

      <ul>
      <% revision_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
