<%= form_with(model: reason_refusal_type, local: true) do |form| %>
  <% if reason_refusal_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reason_refusal_type.errors.count, "error") %> prohibited this reason_refusal_type from being saved:</h2>

      <ul>
      <% reason_refusal_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
